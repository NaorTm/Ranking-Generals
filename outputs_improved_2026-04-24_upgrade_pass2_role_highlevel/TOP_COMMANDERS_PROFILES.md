# Top Commanders Profiles

This profile set is synchronized to `outputs_cleaned_2026-04-11_globaltrust_authoritative`.

## Robust Core

### Alexander Suvorov

- Snapshot: baseline `3.0`, battle-only `5.0`, hierarchical `2.0`.
- Why it matters: `24` strict engagements, `18` strict battle pages, `16` distinct conflicts, `20` known outcomes.
- Exposure and caveats: `B=18; O=0; C=1; W=5`. `V=18; D=2; N=0; U=4`. Stability `core_stable`. No explicit caution flag.

### Suleiman the Magnificent

- Snapshot: baseline `16.0`, battle-only `10.0`, hierarchical `1.0`.
- Why it matters: `34` strict engagements, `15` strict battle pages, `27` distinct conflicts, `24` known outcomes.
- Exposure and caveats: `B=15; O=0; C=2; W=17`. `V=20; D=0; N=4; U=10`. Stability `stable`. No explicit caution flag.

### Napoleon Bonaparte

- Snapshot: baseline `2.0`, battle-only `3.0`, hierarchical `3.0`.
- Why it matters: `74` strict engagements, `63` strict battle pages, `30` distinct conflicts, `62` known outcomes.
- Exposure and caveats: `B=63; O=0; C=6; W=5`. `V=51; D=11; N=0; U=12`. Stability `core_stable`. No explicit caution flag.

### Subutai

- Snapshot: baseline `19.0`, battle-only `25.0`, hierarchical `9.0`.
- Why it matters: `14` strict engagements, `12` strict battle pages, `11` distinct conflicts, `11` known outcomes.
- Exposure and caveats: `B=12; O=0; C=1; W=1`. `V=10; D=1; N=0; U=3`. Stability `stable`. No explicit caution flag.

### Jean Lannes

- Snapshot: baseline `1.0`, battle-only `2.0`, hierarchical `8.0`.
- Why it matters: `29` strict engagements, `24` strict battle pages, `15` distinct conflicts, `26` known outcomes.
- Exposure and caveats: `B=24; O=0; C=1; W=4`. `V=25; D=1; N=0; U=3`. Stability `stable`. No explicit caution flag.

### Henri de La Tour d'Auvergne, Viscount of Turenne

- Snapshot: baseline `15.0`, battle-only `8.0`, hierarchical `12.0`.
- Why it matters: `23` strict engagements, `17` strict battle pages, `13` distinct conflicts, `12` known outcomes.
- Exposure and caveats: `B=17; O=0; C=0; W=6`. `V=11; D=1; N=0; U=11`. Stability `core_stable`. No explicit caution flag.

### Sébastien Le Prestre, Marquis of Vauban

- Snapshot: baseline `8.0`, battle-only `12.0`, hierarchical `20.0`.
- Why it matters: `16` strict engagements, `13` strict battle pages, `7` distinct conflicts, `12` known outcomes.
- Exposure and caveats: `B=13; O=0; C=0; W=3`. `V=12; D=0; N=0; U=4`. Stability `stable`. No explicit caution flag.

### Maurice, Prince of Orange

- Snapshot: baseline `9.0`, battle-only `14.0`, hierarchical `10.0`.
- Why it matters: `39` strict engagements, `34` strict battle pages, `15` distinct conflicts, `33` known outcomes.
- Exposure and caveats: `B=34; O=0; C=0; W=5`. `V=26; D=7; N=0; U=6`. Stability `core_stable`. No explicit caution flag.

## Strong But Model-Sensitive

### Louis XIV

- Snapshot: baseline `24.0`, battle-only `35.0`, hierarchical `4.0`.
- Why it matters: `21` strict engagements, `8` strict battle pages, `14` distinct conflicts, `11` known outcomes.
- Exposure and caveats: `B=8; O=0; C=1; W=12`. `V=9; D=1; N=1; U=10`. Stability `model_sensitive`. No explicit caution flag.

### Louis-Nicolas Davout

- Snapshot: baseline `10.0`, battle-only `7.0`, hierarchical `5.0`.
- Why it matters: `25` strict engagements, `19` strict battle pages, `10` distinct conflicts, `21` known outcomes.
- Exposure and caveats: `B=19; O=0; C=3; W=3`. `V=16; D=5; N=0; U=4`. Stability `core_stable`. No explicit caution flag.

### Babur

- Snapshot: baseline `77.0`, battle-only `52.0`, hierarchical `7.0`.
- Why it matters: `16` strict engagements, `11` strict battle pages, `12` distinct conflicts, `8` known outcomes.
- Exposure and caveats: `B=11; O=0; C=2; W=3`. `V=6; D=1; N=1; U=8`. Stability `highly_model_sensitive`. No explicit caution flag.

### Alexander Farnese, Duke of Parma

- Snapshot: baseline `6.0`, battle-only `9.0`, hierarchical `41.0`.
- Why it matters: `20` strict engagements, `17` strict battle pages, `10` distinct conflicts, `16` known outcomes.
- Exposure and caveats: `B=17; O=0; C=0; W=3`. `V=14; D=2; N=0; U=4`. Stability `model_sensitive`. No explicit caution flag.

### Mehmed II

- Snapshot: baseline `41.0`, battle-only `79.0`, hierarchical `27.0`.
- Why it matters: `25` strict engagements, `19` strict battle pages, `20` distinct conflicts, `18` known outcomes.
- Exposure and caveats: `B=19; O=0; C=1; W=5`. `V=14; D=3; N=1; U=7`. Stability `highly_model_sensitive`. No explicit caution flag.

### Jean Victor Marie Moreau

- Snapshot: baseline `21.0`, battle-only `33.0`, hierarchical `32.0`.
- Why it matters: `25` strict engagements, `22` strict battle pages, `14` distinct conflicts, `21` known outcomes.
- Exposure and caveats: `B=22; O=0; C=1; W=2`. `V=15; D=5; N=1; U=4`. Stability `highly_model_sensitive`. No explicit caution flag.

### Charles XIV John

- Snapshot: baseline `13.0`, battle-only `13.0`, hierarchical `6.0`.
- Why it matters: `27` strict engagements, `16` strict battle pages, `14` distinct conflicts, `21` known outcomes.
- Exposure and caveats: `B=16; O=0; C=3; W=8`. `V=20; D=1; N=0; U=6`. Stability `core_stable`. No explicit caution flag.

### Khalid ibn al-Walid

- Snapshot: baseline `5.0`, battle-only `1.0`, hierarchical `51.0`.
- Why it matters: `40` strict engagements, `39` strict battle pages, `18` distinct conflicts, `32` known outcomes.
- Exposure and caveats: `B=39; O=0; C=0; W=1`. `V=31; D=0; N=1; U=8`. Stability `highly_model_sensitive`. No explicit caution flag.

## Caution / Audit Cases

### Flavius Aetius

- Snapshot: baseline `184.0`, battle-only `107.0`, hierarchical `13.0`.
- Why it matters: `13` strict engagements, `6` strict battle pages, `8` distinct conflicts, `9` known outcomes.
- Exposure and caveats: `B=6; O=0; C=1; W=6`. `V=8; D=0; N=1; U=4`. Stability `highly_model_sensitive`. No explicit caution flag.

### Qasem Soleimani

- Snapshot: baseline `NA`, battle-only `292.0`, hierarchical `17.0`.
- Why it matters: `13` strict engagements, `6` strict battle pages, `12` distinct conflicts, `4` known outcomes.
- Exposure and caveats: `B=6; O=3; C=2; W=2`. `V=4; D=0; N=0; U=9`. Stability `highly_model_sensitive`. higher_level_dependent

### Petar Bojović

- Snapshot: baseline `NA`, battle-only `245.0`, hierarchical `15.0`.
- Why it matters: `11` strict engagements, `4` strict battle pages, `9` distinct conflicts, `8` known outcomes.
- Exposure and caveats: `B=4; O=3; C=1; W=3`. `V=8; D=0; N=0; U=3`. Stability `highly_model_sensitive`. higher_level_dependent

### Živojin Mišić

- Snapshot: baseline `323.0`, battle-only `200.0`, hierarchical `21.0`.
- Why it matters: `11` strict engagements, `5` strict battle pages, `10` distinct conflicts, `8` known outcomes.
- Exposure and caveats: `B=5; O=3; C=2; W=1`. `V=8; D=0; N=0; U=3`. Stability `highly_model_sensitive`. No explicit caution flag.

### Stepa Stepanović

- Snapshot: baseline `174.0`, battle-only `99.0`, hierarchical `16.0`.
- Why it matters: `14` strict engagements, `6` strict battle pages, `10` distinct conflicts, `8` known outcomes.
- Exposure and caveats: `B=6; O=3; C=2; W=3`. `V=8; D=0; N=0; U=6`. Stability `highly_model_sensitive`. No explicit caution flag.

### Peter the Great

- Snapshot: baseline `234.0`, battle-only `377.0`, hierarchical `58.0`.
- Why it matters: `15` strict engagements, `7` strict battle pages, `11` distinct conflicts, `14` known outcomes.
- Exposure and caveats: `B=7; O=0; C=1; W=7`. `V=11; D=2; N=1; U=1`. Stability `highly_model_sensitive`. No explicit caution flag.
